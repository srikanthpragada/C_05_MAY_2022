// Display whether two values are same

#include <stdio.h>

void main()
{
  int a,b;

     printf("Enter two values:");
     scanf("%d%d",&a, &b);

     if(a == b)
        printf("Same");
     else
        printf("Not same");
}
